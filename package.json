{
  "name": "onlygroceries",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm run -F web -F server dev",
    "build": "pnpm run -r build",
    "preview": "pnpm run -r preview"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "web": "^0.0.2"
  }
}
